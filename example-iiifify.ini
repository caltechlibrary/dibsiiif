# CONFIGURATION FILE FOR `iiifify.sh`

# USAGE:
# copy this file to `iiifify.ini` and edit values as appropriate

# NOTE:
# because of the way this file is sourced in `iiifify.sh` use only
# bash-style key-value pairs; do not use ini-style [sections] or
# ini-style ;semicolon comments

# set the correct path the the python binary
PYTHON="/path/to/virtual/environment/python"
